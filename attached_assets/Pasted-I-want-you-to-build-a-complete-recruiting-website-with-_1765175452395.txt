I want you to build a complete recruiting website with an admin dashboard for content management, using the following stack:

📌 Tech Stack Requirements

Next.js 15 (App Router)

TypeScript

TailwindCSS

Prisma ORM

PostgreSQL (Supabase)

NextAuth (Credentials provider) for admin login

Fully bilingual (Vietnamese + English)

File structure compatible with Replit hosting

Admin panel protected with middleware

Landing page pulls dynamic content from the database

1️⃣ PROJECT STRUCTURE

Please generate the following folder/file layout:

app/
  layout.tsx
  page.tsx

  admin/
    layout.tsx
    login/page.tsx
    hero/
      page.tsx
      HeroForm.tsx
    jobs/
      page.tsx
      new/page.tsx
      [id]/page.tsx
      JobForm.tsx

components/
  LandingPage.tsx

lib/
  prisma.ts

prisma/
  schema.prisma
  seed.mjs

public/
  (assets)

2️⃣ DATABASE MODELS (PRISMA)

Create a Prisma schema with these models:

AdminUser

id

email

password (hashed)

name

Hero

(bilingual hero section)

title_vi

title_en

subtitle_vi

subtitle_en

backgroundType ("image" | "video")

imageUrl

videoUrl

ctaText_vi

ctaText_en

ctaLink

Job

(bilingual job postings)

jobTitle_vi

jobTitle_en

department_vi

department_en

location_vi

location_en

salary_vi

salary_en

description_vi

description_en

requirements_vi

requirements_en

applyEmail

applyLink

isActive (boolean)

You must generate the correct schema.prisma file.

3️⃣ DATABASE CONNECTION

Use:

DATABASE_URL="postgresql://<user>:<password>@<supabase-pool-host>:5432/postgres?pgbouncer=true"
DIRECT_URL="postgresql://<user>:<password>@<supabase-direct-host>:5432/postgres"


Add:

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}

4️⃣ PRISMA CLIENT HELPER

Create lib/prisma.ts:

import { PrismaClient } from "@prisma/client";

const globalForPrisma = globalThis as unknown as {
  prisma: PrismaClient | undefined;
};

export const prisma =
  globalForPrisma.prisma ??
  new PrismaClient({
    log: ["error", "warn"],
  });

if (process.env.NODE_ENV !== "production") globalForPrisma.prisma = prisma;

5️⃣ AUTHENTICATION (NextAuth)

Implement admin login:

Credentials provider

Email + password

Passwords must be hashed with bcrypt

Only one admin user is needed (seeded)

Create login page /admin/login and configure middleware so that:

✔ Logged-out users are redirected to /admin/login
✔ Logged-in users can access /admin/... pages

6️⃣ ADMIN DASHBOARD (CRUD)

Build full CRUD interfaces:

Hero Editor (/admin/hero)

Load hero data from Prisma

Update or create if missing

Bilingual fields

Jobs CRUD (/admin/jobs)

List all jobs

Create new job

Edit existing job (bilingual)

Toggle active/inactive

API routes for POST, PUT, DELETE

All admin screens must be clean, simple Tailwind forms.

7️⃣ PUBLIC LANDING PAGE (/)

Create a modern, mobile-friendly landing page:

Features:

VI ↔ EN toggle button

Pull hero + job list from database

Hero with background image/video + CTA button

Job cards showing bilingual text

“Apply now” button linking to applyLink or mailto:applyEmail

Styled similar to: https://coke-ver2.monamedia.net/
 (clean recruiting site style)

Use Framer Motion for subtle animations.

8️⃣ SEED SCRIPT

Create prisma/seed.mjs to:

Insert admin user (with bcrypt-hashed password)

Insert a default hero section

9️⃣ FINAL TASKS

Ensure all imports use alias @/*

Generate correct tsconfig.json with:

"baseUrl": ".",
"paths": {
  "@/*": ["./*"]
}


Ensure the app builds successfully with:

npx prisma generate
npx prisma migrate dev
npm run dev

🔥 Your goal

Generate ALL source code required:

Every file listed above

Tailwind config

NextAuth config

Prisma schema

All Admin pages

All API routes

Landing page

Seed script

tsconfig.json updates

next.config.js if needed

The final output should be a fully working recruiting website with admin dashboard, bilingual support, CRUD management, and a dynamic landing page ready to host on Replit.

📌 IMPORTANT

Please generate COMPLETE code for each file, not summaries.